# 最小摄取闭环测试说明

这份文档只测试 `Phase 2` 已经完成的最小离线摄取闭环：

```text
PDF -> Document -> Chunks -> Embeddings -> BM25 -> Chroma
```

对应代码文件：

- `scripts/ingest.py`
- `src/modular_rag_repro/ingestion/pdf_loader.py`
- `src/modular_rag_repro/ingestion/chunker.py`
- `src/modular_rag_repro/ingestion/embedding_encoder.py`
- `src/modular_rag_repro/ingestion/bm25_indexer.py`
- `src/modular_rag_repro/ingestion/chroma_upserter.py`
- `src/modular_rag_repro/ingestion/pipeline.py`

## 测试前提

先进入 `repro/` 目录：

```powershell
cd "D:\OneDrive - stu.scau.edu.cn\桌面\workspace\MODULAR-RAG-MCP-SERVER\repro"
```

当前复现版临时复用外层项目的虚拟环境，所以统一用：

```powershell
..\.venv\Scripts\python.exe
```

另外，`Ollama` 需要已经启动，并且本地已有 embedding 模型：

- `nomic-embed-text`

## 测试 1：执行一次最小摄取

```powershell
..\.venv\Scripts\python.exe scripts\ingest.py --path ..\tests\fixtures\sample_documents\simple.pdf --collection phase2-test-a
```

预期输出里至少应包含：

```text
[OK] doc_id=...
[OK] chunk_count=1
[OK] vector_count=1
[OK] upserted_count=1
```

含义：

- `doc_id`：文档 ID 已成功生成
- `chunk_count=1`：这个 PDF 被切成了 1 个 chunk
- `vector_count=1`：1 个 chunk 成功生成了 1 个向量
- `upserted_count=1`：这 1 条向量已经写进 Chroma

## 测试 2：验证 BM25 索引真的落盘并可查询

```powershell
..\.venv\Scripts\python.exe -c "from pathlib import Path; import sys; sys.path.insert(0, str(Path('src').resolve())); from modular_rag_repro.ingestion import BM25Indexer; idx=BM25Indexer(index_dir='data/db/bm25'); print('loaded=', idx.load('phase2-test-a')); results=idx.query('sample pdf', top_k=3); print('result_count=', len(results)); print('top_chunk_id=', results[0].chunk_id if results else None)"
```

预期输出类似：

```text
loaded= True
result_count= 1
top_chunk_id= doc_xxx_0000_xxxx
```

含义：

- `loaded=True`：磁盘上的 BM25 索引文件已成功读回
- `result_count=1`：关键词查询命中了 1 条 chunk

## 测试 3：验证 Chroma 里真的有向量

```powershell
..\.venv\Scripts\python.exe -c "from pathlib import Path; import sys; sys.path.insert(0, str(Path('src').resolve())); from modular_rag_repro.settings import load_settings; from modular_rag_repro.ingestion import ChromaUpserter; s=load_settings(); u=ChromaUpserter(s, collection='phase2-test-a'); print('collection_count=', u.get_collection_count('phase2-test-a'))"
```

预期输出类似：

```text
collection_count= 1
```

含义：

- `collection_count=1`：Chroma collection 中已有 1 条向量记录

## 测试 4：验证失败路径

```powershell
..\.venv\Scripts\python.exe scripts\ingest.py --path ..\tests\fixtures\sample_documents\missing.pdf --collection phase2-test-b
```

预期结果：

- 输出明确的路径不存在或文件发现失败信息
- 进程退出码不是 `0`

## 如何判断这一阶段通过

满足下面 4 条，就说明当前最小摄取闭环是可用的：

1. `ingest.py` 能成功处理 `simple.pdf`
2. `BM25Indexer` 能查到结果
3. `Chroma` collection 计数大于 `0`
4. 错误路径能稳定返回可读错误

## 注意事项

- 测试 `Chroma` 时尽量串行跑，不要同时开多条命令访问同一个持久化目录。
- `--force` 现在只是保留接口含义，`SHA256` 去重还没实现。
- 当前只验证最小闭环，不代表 Query、MCP、Dashboard 已经完成。
