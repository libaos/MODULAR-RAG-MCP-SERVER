# 功能矩阵

## 目的

这份文件用来记录“参考实现已经做了什么”，后面的复现代码按这里对齐，不按感觉乱做。

## 参考入口

| 类别 | 入口 | 作用 |
|---|---|---|
| CLI | `scripts/ingest.py` | 文档摄取 |
| CLI | `scripts/query.py` | 混合检索查询 |
| CLI | `scripts/evaluate.py` | Golden Set 评估 |
| CLI | `scripts/start_dashboard.py` | 启动 Dashboard |
| MCP | `src/mcp_server/server.py` | MCP Server 真正入口 |
| MCP Tools | `src/mcp_server/tools/` | 3 个已注册工具 |
| Dashboard | `src/observability/dashboard/app.py` | 六页入口 |

## 功能矩阵

| 模块 | 已实现功能 | 代码证据 | 复现优先级 | 当前边界 |
|---|---|---|---|---|
| 配置系统 | `llm`、`embedding`、`vision_llm`、`vector_store`、`retrieval`、`rerank`、`evaluation`、`observability`、`ingestion` 九大配置块 | `config/settings.yaml` | P0 | 当前默认是本地优先配置，很多增强能力默认关闭 |
| 摄取入口 | 支持单文件/目录摄取、`collection`、`force`、`dry-run`、`verbose` | `scripts/ingest.py` | P0 | 当前 CLI 的文件发现主路径是 PDF-first |
| 文件去重 | 基于 SHA256 的摄取幂等检查 | `src/ingestion/pipeline.py`、`src/libs/loader/file_integrity.py` | P0 | 当前去重是全局 `file_hash`，不是 `file_hash + collection` |
| 文档加载 | PDF 文本抽取、图片提取、生成 `Document` | `src/libs/loader/pdf_loader.py`、`src/ingestion/pipeline.py` | P0 | 真实主链路是 PDF-first，其他格式成熟度不一致 |
| 文本分块 | 按配置切 chunk，产出稳定 `chunk_id` 和元数据 | `src/ingestion/chunking/document_chunker.py` | P0 | 首版复现只需先做最小 chunker |
| Transform | `ChunkRefiner`、`MetadataEnricher`、`ImageCaptioner` 三段式转换 | `src/ingestion/transform/` | P1 | 其中 LLM/Vision 增强支持关闭和失败降级 |
| 编码 | Dense Embedding + Sparse 编码 | `src/ingestion/embedding/dense_encoder.py`、`src/ingestion/embedding/sparse_encoder.py` | P0 | Dense 依赖 embedding provider，Sparse 走 BM25 语料统计 |
| 存储 | Chroma 向量写入、BM25 索引写入、图片文件+SQLite 索引 | `src/ingestion/storage/` | P0 | 首版复现可先只做 Chroma + BM25，图片后补 |
| 查询入口 | 支持 `query`、`collection`、`top-k`、`verbose`、`no-rerank` | `scripts/query.py` | P0 | 当前输出是“检索结果片段”，不是自由生成回答 |
| QueryProcessor | 关键词提取、过滤器解析 | `src/core/query_engine/query_processor.py` | P0 | 首版可先做最小版本 |
| Dense 检索 | 基于 embedding 的向量召回 | `src/core/query_engine/dense_retriever.py` | P0 | 与 Chroma、EmbeddingFactory 配合 |
| Sparse 检索 | 基于 BM25 的关键词召回 | `src/core/query_engine/sparse_retriever.py` | P0 | 中英混合场景下表现依赖分词和语料 |
| 融合 | RRF 融合 Dense + Sparse 结果 | `src/core/query_engine/fusion.py`、`src/core/query_engine/hybrid_search.py` | P0 | 是当前检索主路径，不是可有可无的装饰层 |
| 回退机制 | Dense/Sparse 单路失败时自动回退到另一条路径 | `src/core/query_engine/hybrid_search.py` | P0 | 两路都失败才抛错 |
| Rerank | 支持 `none / cross_encoder / llm`，异常时回退原顺序 | `src/core/query_engine/reranker.py`、`src/libs/reranker/` | P1 | 当前本地默认关闭 |
| 响应构建 | 返回文本结果、citation、structured metadata、可选图片块 | `src/core/response/response_builder.py` | P1 | 不是完整生成式回答链路，更偏检索结果封装 |
| MCP Server | stdio + JSON-RPC + `tools/list` + `tools/call` | `src/mcp_server/server.py`、`src/mcp_server/protocol_handler.py` | P0 | `main.py` 不是主入口，真正入口在 `src/mcp_server/server.py` |
| MCP Tool 1 | `query_knowledge_hub`：执行主查询链路，返回 citation 和可选图片 | `src/mcp_server/tools/query_knowledge_hub.py` | P0 | 首版复现优先实现这个 |
| MCP Tool 2 | `list_collections`：列出现有集合，可带统计 | `src/mcp_server/tools/list_collections.py` | P0 | 依赖 Chroma |
| MCP Tool 3 | `get_document_summary`：按文档聚合标题、摘要、标签、来源 | `src/mcp_server/tools/get_document_summary.py` | P0 | 依赖 Chroma 中的 chunk/metadata 聚合 |
| Dashboard | 6 页：Overview / Data Browser / Ingestion Manager / Ingestion Traces / Query Traces / Evaluation Panel | `src/observability/dashboard/app.py` | P0 | 首版可先做“能开页+能显示数据”，不追求 UI 细节 |
| Trace | ingestion/query 阶段记录到 JSONL | `src/core/trace/`、`config/settings.yaml` | P1 | `observability.trace_enabled=true` 时启用 |
| 评估 | `evaluate.py` + `EvalRunner` + evaluator factory + golden set | `scripts/evaluate.py`、`src/observability/evaluation/` | P1 | `ragas` 是增强项，不应阻塞主链路 |
| 生命周期管理 | 文档列表、详情、删除、集合统计，跨 Chroma/BM25/Image/Integrity 协调 | `src/ingestion/document_manager.py` | P1 | 首版复现可后置到 Phase 7 |

## 最小复现闭环

Phase 0 明确后，复现时只先盯住下面这条链路：

```text
PDF -> Loader -> Chunker -> Dense/Sparse Encode -> Chroma/BM25
query -> Dense + Sparse -> RRF -> 输出结果
```

只要这条链路跑通，后面的 MCP、Dashboard、Trace、评估都只是往外包一层能力。

## 当前已知边界

- `main.py` 不是参考实现的真正 MCP 入口。
- 当前去重逻辑不是按 collection 作用域设计。
- 默认配置关闭了 `vision_llm`、`chunk_refiner.use_llm`、`metadata_enricher.use_llm`、`rerank`、`evaluation`。
- 当前主系统更偏“知识检索服务”，不是一个完整的生成式问答 Agent。
