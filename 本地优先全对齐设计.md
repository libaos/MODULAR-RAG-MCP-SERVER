# 本地优先全对齐设计

## 1. 目标

这份设计针对 `repro/` 当前“核心主线已对齐，但增强项仍为简化版”的状态，继续补齐以下能力：

- 生成式最终回答
- provider 化的增强链路
- 可选 LLM `ChunkRefiner`
- 可选 LLM `MetadataEnricher`
- 可选 Vision `ImageCaptioner`
- 可选更强 `Reranker`
- 更完整的 Dashboard 多模态展示

设计原则是：

- 默认本地可跑
- 所有增强项都有 fallback
- 不破坏现有 `rule/simple/local` 主链路
- 配置结构向原项目对齐

---

## 2. 当前问题

`repro/` 现在已经有完整主线：

```text
PDF
-> ImageStorage
-> Chunker
-> ChunkRefiner
-> ImageCaptioner
-> MetadataEnricher
-> Embedding
-> BM25
-> Chroma
-> Dense / Sparse / RRF / SimpleRerank
-> CLI / MCP / Dashboard
```

但仍有几个明显的“简化版”缺口：

1. `ChunkRefiner` 只有规则版
2. `MetadataEnricher` 只有规则版
3. `ImageCaptioner` 只有规则版
4. `Reranker` 只有 simple 版本
5. 查询没有生成式最终回答
6. Dashboard 的图片与回答展示还偏基础

这意味着：

- 核心工程闭环已经存在
- 但还没有把“增强能力的接口和 fallback 体系”补齐

---

## 3. 设计总览

本次补齐不直接把系统改成“强依赖外部模型”的形态，而是引入统一 provider 接口：

```text
provider = local/rule/simple  -> 默认可用
provider = ollama/openai/...  -> 可选增强
失败 -> fallback 回本地默认实现
```

也就是说，增强项不是替换主链路，而是叠加在主链路之上。

---

## 4. 模块设计

### 4.1 摄取增强层

这部分保留现有文件边界，不推翻已实现结构。

#### ChunkRefiner

- 文件：`src/modular_rag_repro/ingestion/chunk_refiner.py`
- 现状：规则版
- 补齐后：
  - `provider=rule|llm`
  - `use_llm=true` 时，优先走 LLM 精炼
  - LLM 不可用或失败时，退回规则版

metadata 增加：

- `refined_by`
- `refiner_mode`
- `refinement_changed`
- `refiner_fallback`

#### MetadataEnricher

- 文件：`src/modular_rag_repro/ingestion/metadata_enricher.py`
- 现状：规则版
- 补齐后：
  - `provider=rule|llm`
  - `use_llm=true` 时，尝试生成更自然的 `summary/tags/title`
  - 失败时退回规则版

metadata 增加：

- `enriched_by`
- `metadata_enricher_mode`
- `enricher_fallback`

#### ImageCaptioner

- 文件：`src/modular_rag_repro/ingestion/image_captioner.py`
- 现状：规则版
- 补齐后：
  - `provider=rule|vision_llm`
  - 当 `vision_llm.enabled=true` 且 provider 可用时，调用视觉描述
  - 失败时退回规则版 caption

metadata 增加：

- `captioned_by`
- `image_captioner_mode`
- `caption_fallback`

---

### 4.2 查询增强层

#### Reranker

- 文件：`src/modular_rag_repro/query_engine/reranker.py`
- 现状：`SimpleReranker`
- 补齐后：
  - 重构成统一入口
  - `provider=simple|cross_encoder|llm`
  - `cross_encoder` 第一版允许先做“本地占位 + 明确 fallback”
  - `llm` 第一版允许走本地 `ollama` 文本打分

统一行为：

- 任一增强 provider 失败，回退到 simple / fusion
- trace 里明确记录 `provider/mode/fallback/error`

#### AnswerGenerator

- 新文件：`src/modular_rag_repro/query_engine/answer_generator.py`
- 职责：
  - 根据最终检索结果生成最终回答
  - 支持 `provider=none|ollama|openai`
  - 默认 `none`
  - 打开后优先生成回答，失败时回退到结构化检索结果

输出层面会补两种内容：

- `generated_answer`
- `answer_mode=generated|fallback_retrieval_only`

接入点：

- `query_engine/workflow.py`
- `scripts/query.py`
- MCP `query_knowledge_hub`

---

### 4.3 响应与多模态层

#### ResponseFormatter

- 文件：`src/modular_rag_repro/response/response_formatter.py`
- 补齐点：
  - 输出最终回答字段
  - 输出“检索摘要”和“生成式回答”双视图
  - 区分：
    - `retrieval_summary`
    - `generated_answer`

#### MultimodalAssembler

- 文件：`src/modular_rag_repro/response/multimodal_assembler.py`
- 补齐点：
  - 在 MCP 中继续返回图片块
  - 如果有最终回答，先返回回答文本，再跟 citation 和图片块

---

### 4.4 Dashboard

Dashboard 不新增页面，继续沿用现有六页结构。

重点补强：

- `Overview`
  - 展示生成式回答能力是否开启
- `Data Browser`
  - 展示更多图片字段
  - 支持更完整图片预览
- `Query Traces`
  - 展示 `rerank` 和 `answer_generation` 阶段
- `Evaluation Panel`
  - 展示“检索式结果”和“生成式结果”评估区别

---

## 5. 配置设计

配置保留原有结构，新增但不破坏兼容性。

### 5.1 `llm`

增加可供最终回答、LLM 增强器复用的字段：

- `enabled`
- `timeout_seconds`

### 5.2 `vision_llm`

增加：

- `base_url`
- `timeout_seconds`
- `fallback_to_rule`

### 5.3 `rerank`

扩展为：

- `enabled`
- `provider`
- `model`
- `top_k`
- `timeout_seconds`
- `fallback_provider`

### 5.4 `ingestion.chunk_refiner`

扩展为：

- `use_llm`
- `provider`
- `model`
- `timeout_seconds`

### 5.5 `ingestion.metadata_enricher`

扩展为：

- `use_llm`
- `provider`
- `model`
- `timeout_seconds`

### 5.6 `answer_generation`

新增顶层配置：

- `enabled`
- `provider`
- `model`
- `max_context_chunks`
- `timeout_seconds`

---

## 6. 错误处理与 fallback

这是这次补齐最关键的要求。

统一规则：

- 默认 provider 永远可用
- 外部增强 provider 只能“加分”，不能让主链路不可用
- 每次 fallback 都要：
  - 写 trace
  - 写 metadata
  - 尽量在 CLI / MCP / Dashboard 中可见

示例：

- Vision caption 失败 -> 回规则版 caption
- LLM refiner 失败 -> 回规则版 refiner
- LLM enrich 失败 -> 回规则版 enrich
- LLM rerank 失败 -> 回 simple rerank 或 fusion
- Answer generation 失败 -> 回 retrieval-only response

---

## 7. 测试策略

新增测试分两层：

### 7.1 本地默认链路

必须稳定通过：

- 不依赖云
- 不依赖视觉模型
- 可在 `ollama` 或 mock 环境下跑

### 7.2 provider 增强链路

使用 mock / monkeypatch 验证：

- provider 被调用
- 失败时 fallback
- trace 记录正确

重点测试点：

- `AnswerGenerator`
- `LLM refiner` fallback
- `LLM enricher` fallback
- `Vision caption` fallback
- `LLM rerank` fallback
- MCP 返回生成答案 + 图片块

---

## 8. 实施顺序

为了避免一次改太多，按下面顺序推进：

1. `AnswerGenerator`
2. `provider 化 Reranker`
3. `LLM ChunkRefiner`
4. `LLM MetadataEnricher`
5. `Vision ImageCaptioner`
6. `Dashboard 多模态补强`
7. `文档与测试收口`

---

## 9. 完成标准

只有满足以下条件，才算“本地优先全对齐”完成：

- 默认本地模式完整可跑
- 打开增强配置时，增强链路可工作或明确 fallback
- CLI / MCP / Dashboard 都能看到最终回答与多模态结果
- trace 能体现增强阶段
- 全量测试通过
- 对齐表更新为“仅剩高配 provider 质量差异，不剩结构能力缺口”
