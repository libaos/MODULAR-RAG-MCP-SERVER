# 原项目对齐表

这份表的目的不是说“像不像”，而是明确：

- 原项目这一块做了什么
- `repro/` 当前做到什么程度
- 哪些已经对齐
- 哪些还是简化版

---

## 1. 总体判断

如果按“核心工程主线 + 本地优先增强链路”来算，`repro/` 现在已经对齐到很高的程度了：

- `摄取 -> 检索 -> MCP -> Dashboard -> Trace -> 评估 -> 文档管理` 这条主线已经完整
- 大多数高频面试点已经有对应实现
- 剩下主要是“provider 质量和生态范围差异”，不是“结构能力缺失”

更适合的结论是：

> `repro` 现在已经完成了“核心功能对齐 + 本地优先增强能力对齐”的复现状态。

---

## 2. 对齐总表

| 模块 | 原项目 | `repro` 当前状态 | 是否对齐 | 差距 / 备注 |
| --- | --- | --- | --- | --- |
| 配置结构 | 有 `llm / embedding / vision_llm / vector_store / retrieval / rerank / evaluation / observability / ingestion` | 已保留同级结构 | 基本对齐 | 细分 provider 没原项目那么多 |
| CLI 入口 | `ingest / query / evaluate / start_dashboard` | 4 个入口都已实现 | 对齐 | 行为更轻量 |
| PDF 摄取 | 支持 PDF-first 摄取 | 已实现 | 对齐 | 当前仍是 PDF-first |
| SHA256 去重 | 有幂等处理 | 已实现 | 对齐 | 目前是 SQLite 记录版 |
| 文本分块 | 有 chunk + overlap | 已实现 | 对齐 | 分块策略更简化 |
| ChunkRefiner | 有规则 + 可选 LLM | 已实现规则版 + 可选 LLM + fallback | 基本对齐 | 质量仍以本地优先为主 |
| MetadataEnricher | 有规则 + 可选 LLM | 已实现规则版 + 可选 LLM + fallback | 基本对齐 | 质量仍以本地优先为主 |
| 图片抽取 | 可抽图片并建索引 | 已实现 | 对齐 | 已可落盘和索引 |
| ImageCaptioner | 有 Vision LLM caption | 已实现规则版 + 可选 Vision provider + fallback | 基本对齐 | 视觉质量仍不是完整云端版 |
| Embedding | 有 Dense 编码 | 已实现 Ollama embedding | 对齐 | provider 更少 |
| BM25 | 有 Sparse 索引 | 已实现 | 对齐 | 本地 JSON 版索引 |
| Chroma 向量库 | 有本地 Chroma | 已实现 | 对齐 | 只保留 Chroma 主线 |
| Dense 检索 | 有 | 已实现 | 对齐 | 基础版 |
| Sparse 检索 | 有 | 已实现 | 对齐 | 基础版 |
| RRF Fusion | 有 | 已实现 | 对齐 | 基础版 |
| Rerank | 原项目支持 none / cross-encoder / llm 等 | 已实现 `simple / llm / cross_encoder` + fallback | 基本对齐 | 仍以 simple 为最稳默认 |
| 最终回答 | 原项目更偏检索结果返回，后续可接回答生成 | 已实现 `local / ollama` 生成式最终回答 | 基本对齐 | 不是 Agent 化长回答 |
| MCP Server | 有 stdio + 3 个 tools | 已实现 | 对齐 | 工具集合已对齐主线 |
| MCP 多模态 | 可返回文本 + 图片 | 已实现文本 + 生成回答 + 图片块返回 | 对齐 | 图片块仍偏基础 |
| Dashboard 六页 | 有 | 已实现 | 对齐 | 已补回答状态与图片预览，交互仍偏轻量 |
| Trace | ingest/query 都有 | 已实现 | 对齐 | 记录粒度比原项目轻一些 |
| Golden Set 评估 | 有 | 已实现 | 对齐 | 先做最小闭环 |
| Ragas / 更复杂评估 | 原项目有更完整扩展方向 | 当前没有完整增强版 | 未完全对齐 | 这是主要剩余增强项 |
| 文档摘要 | 有 | 已实现 | 对齐 | 已带 `image_count` 等信息 |
| 文档删除 | 有 | 已实现 | 对齐 | Chroma/BM25/图片/去重记录都会清理 |
| 索引重建 | 有 | 已实现 | 对齐 | 先做 BM25 重建钩子 |
| 测试体系 | 原项目有较完整测试 | 已实现 unit/integration/e2e | 对齐 | 当前全量 `62 passed` |

---

## 3. 现在已经完全不算“缺”的部分

这些你现在可以直接认为已经补上了，不需要再说“还没复现”：

- PDF 摄取
- 去重
- chunking
- embedding
- BM25
- Chroma
- Dense / Sparse / RRF
- MCP 3 工具
- Dashboard 六页
- trace
- Golden Set 评估
- 文档删除
- 索引重建
- 图片落盘和图片索引

---

## 4. 现在属于“结构已对齐，但质量仍有差异”的部分

这些不是没有，而是“第一版已经能用，但比原项目弱一点”：

- `ChunkRefiner`
  - 已有 `rule / llm` 路径
  - 但默认还是规则版最稳

- `MetadataEnricher`
  - 已有 `rule / llm` 路径
  - 但增强摘要质量仍取决于本地模型

- `ImageCaptioner`
  - 已有 `rule / vision` 路径
  - 但视觉 caption 仍是本地优先能力，不是完整高配版

- `Rerank`
  - provider 已补齐
  - 默认还是 `simple` 最稳

- `生成式最终回答`
  - 已经补上
  - 但仍以检索增强问答为主，不是复杂 Agent answer chain

- `多模态展示`
  - 现在 MCP 和 Dashboard 都已能展示图片与回答
  - 但交互深度仍低于高配工作台

---

## 5. 现在真正还可以继续补的点

如果你还要继续往“更像原项目”推进，优先级建议是：

1. `更完整的评估体系（例如 Ragas 增强）`
2. `更强的云端 / 高配 provider`
3. `更复杂的生成式回答链路`
4. `Dashboard 更完整图片浏览和交互`

---

## 6. 一句话结论

你现在可以这样理解：

> `repro` 已经把原项目最重要的工程闭环和本地优先增强链路都复现出来了，剩下主要是高配 provider 质量差异，而不是结构能力缺口。
