# 本地优先全对齐实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `repro/` 剩余的增强项补成“默认本地可跑、增强 provider 可选、失败有 fallback”的全对齐状态。

**Architecture:** 保留现有主链路，在 `ChunkRefiner / MetadataEnricher / ImageCaptioner / Reranker / QueryWorkflow / ResponseFormatter / MCP / Dashboard` 上引入 provider 化增强路径和统一 fallback。默认仍走 `rule/simple/local`，增强 provider 只在配置开启时生效。

**Tech Stack:** Python, Ollama HTTP API, Streamlit, MCP Python SDK, pytest

---

### Task 1: 生成式最终回答

**Files:**
- Create: `src/modular_rag_repro/query_engine/answer_generator.py`
- Modify: `src/modular_rag_repro/query_engine/workflow.py`
- Modify: `src/modular_rag_repro/response/response_formatter.py`
- Modify: `src/modular_rag_repro/types.py`
- Modify: `scripts/query.py`
- Modify: `src/modular_rag_repro/mcp_server/tools/query_knowledge_hub.py`
- Test: `tests/unit/test_answer_generator.py`
- Test: `tests/integration/test_query_workflow.py`
- Test: `tests/e2e/test_query_cli_answer_generation.py`

- [ ] 写 `AnswerGenerator` 的失败测试和 fallback 测试
- [ ] 跑失败测试，确认当前没有生成式回答能力
- [ ] 实现 `provider=none|ollama`
- [ ] 接入 `QueryWorkflow` 与 `ResponseFormatter`
- [ ] 更新 CLI / MCP 输出
- [ ] 跑相关测试直到通过

### Task 2: provider 化 Reranker

**Files:**
- Modify: `src/modular_rag_repro/query_engine/reranker.py`
- Modify: `src/modular_rag_repro/query_engine/workflow.py`
- Modify: `src/modular_rag_repro/settings.py`
- Modify: `config/settings.yaml`
- Test: `tests/unit/test_reranker.py`
- Test: `tests/integration/test_query_workflow.py`

- [ ] 先写 `provider` 选择与 fallback 的测试
- [ ] 跑失败测试
- [ ] 把 `SimpleReranker` 重构成统一入口
- [ ] 增加 `llm` / `cross_encoder` 占位增强实现与 fallback
- [ ] 验证 trace 中 `provider/mode/fallback/error` 正确

### Task 3: LLM ChunkRefiner

**Files:**
- Modify: `src/modular_rag_repro/ingestion/chunk_refiner.py`
- Modify: `src/modular_rag_repro/settings.py`
- Modify: `config/settings.yaml`
- Modify: `src/modular_rag_repro/ingestion/pipeline.py`
- Test: `tests/unit/test_chunk_refiner.py`
- Test: `tests/integration/test_ingestion_pipeline.py`

- [ ] 先写 `use_llm=true` 与失败回退测试
- [ ] 跑失败测试
- [ ] 增加 `provider=rule|llm`
- [ ] 实现最小 `ollama` refiner 调用
- [ ] 验证 fallback metadata 和 trace

### Task 4: LLM MetadataEnricher

**Files:**
- Modify: `src/modular_rag_repro/ingestion/metadata_enricher.py`
- Modify: `src/modular_rag_repro/settings.py`
- Modify: `config/settings.yaml`
- Modify: `src/modular_rag_repro/ingestion/pipeline.py`
- Test: `tests/unit/test_metadata_enricher.py`
- Test: `tests/integration/test_ingestion_pipeline.py`

- [ ] 先写增强路径与 fallback 测试
- [ ] 跑失败测试
- [ ] 增加 `provider=rule|llm`
- [ ] 实现最小 `ollama` enrich 调用
- [ ] 验证元数据与 trace

### Task 5: Vision ImageCaptioner

**Files:**
- Modify: `src/modular_rag_repro/ingestion/image_captioner.py`
- Modify: `src/modular_rag_repro/settings.py`
- Modify: `config/settings.yaml`
- Modify: `src/modular_rag_repro/ingestion/pipeline.py`
- Test: `tests/unit/test_image_captioner.py`
- Test: `tests/integration/test_ingestion_pipeline.py`

- [ ] 先写 `vision_llm` 增强路径与 fallback 测试
- [ ] 跑失败测试
- [ ] 增加 `provider=rule|vision_llm`
- [ ] 实现最小视觉 caption provider 接口
- [ ] 验证图片 caption fallback

### Task 6: Dashboard 多模态补强

**Files:**
- Modify: `src/modular_rag_repro/dashboard/services.py`
- Modify: `src/modular_rag_repro/dashboard/pages.py`
- Modify: `src/modular_rag_repro/query_engine/workflow.py`
- Test: `tests/e2e/test_dashboard_smoke.py`

- [ ] 先写页面数据和空态测试
- [ ] 跑失败测试
- [ ] 增加生成式回答状态展示
- [ ] 增加图片预览/图片详情/回答信息展示
- [ ] 确保六页 smoke 仍然通过

### Task 7: 文档与回归

**Files:**
- Modify: `README.md`
- Modify: `原项目对齐表.md`
- Modify: `复现任务清单.md`
- Test: `tests/`

- [ ] 更新 README 里的能力说明和运行方式
- [ ] 更新对齐表，把剩余差距改成“高配质量差异”
- [ ] 跑全量测试
- [ ] 记录最终验证结果
