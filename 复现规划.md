# Modular RAG MCP Server 复现规划

## 目标

按“功能等价、本地优先、分阶段交付”的方式，复现一个可独立运行的 Modular RAG MCP Server。

首版不追求逐文件一致，只要求核心闭环可运行：

- PDF 摄取
- 分块与元数据处理
- Embedding 与 BM25 建索引
- Chroma 检索
- Dense + Sparse + RRF 混合检索
- MCP 工具
- Dashboard 六页
- 摄取与查询 Trace
- Golden Set 评估

## 复现范围

### P0 必须完成

- `scripts/ingest.py`
- `scripts/query.py`
- `scripts/evaluate.py`
- `scripts/start_dashboard.py`
- `query_knowledge_hub`
- `list_collections`
- `get_document_summary`
- Chroma + BM25
- Hybrid Search
- Dashboard 六页结构

### P1 建议补齐

- 图片抽取与图片索引
- 多模态返回
- 可选 rerank
- Golden Set 评估
- Trace 可视化

### P2 可以延后

- Vision Caption
- 多 Provider 切换
- 更深的 Ragas 集成
- 更完整的文档生命周期管理

## 技术路线

| 模块 | 首版方案 | 说明 |
|---|---|---|
| Python | `3.11/3.12 + .venv` | 不依赖 conda |
| LLM | `ollama / qwen2.5:3b` | 本地优先 |
| Embedding | `ollama / nomic-embed-text` | 本地优先 |
| Vector Store | `Chroma` | 与参考实现主路径一致 |
| Sparse 检索 | `BM25 + jieba` | 与参考实现主路径一致 |
| Dashboard | `Streamlit` | 与参考实现一致 |
| MCP | `mcp` Python SDK | stdio + `tools/list` + `tools/call` |
| Trace | `jsonl` | 简单、可读、易调试 |
| Evaluation | `golden set + runner` | Ragas 作为增强项 |

## 分阶段规划

| 阶段 | 目标 | 交付物 | 验收标准 |
|---|---|---|---|
| Phase 0 | 梳理基线 | 功能矩阵 + 验收矩阵 | P0/P1/P2 范围明确 |
| Phase 1 | 工程骨架 | 可运行 CLI 骨架 | `--help` 能正常执行 |
| Phase 2 | 摄取 MVP | PDF -> Chroma/BM25 | 能成功摄取 1 个 PDF |
| Phase 3 | 查询 MVP | Dense + Sparse + RRF | 查询结果包含分数和来源 |
| Phase 4 | MCP | 3 个 stdio 工具 | `tools/list` / `tools/call` 可用 |
| Phase 5 | Dashboard | 六页面板 | 六页都能打开 |
| Phase 6 | Trace 与评估 | Trace + 评估输出 | `evaluate.py` 能生成报告 |
| Phase 7 | 管理能力 | 集合与文档管理 | 删除/摘要功能可用 |
| Phase 8 | 测试与文档 | 测试集 + README | 核心冒烟测试通过 |

## 推荐顺序

```text
配置/类型
-> 摄取
-> 查询
-> MCP
-> Dashboard
-> Trace/评估
-> 管理能力
-> 测试/文档
```

## 执行规则

- 当前阶段没跑通，不进入下一阶段。
- 每个阶段都必须有一个可执行的验收命令。
- 第一条闭环只盯住 `ingest -> query`。
