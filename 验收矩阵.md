# 验收矩阵

## Phase 0 参考样例

| 用途 | 样例路径 |
|---|---|
| 最小 PDF | `tests/fixtures/sample_documents/simple.pdf` |
| 含图片 PDF | `tests/fixtures/sample_documents/with_images.pdf` |
| Golden Set | `tests/fixtures/golden_test_set.json` |
| Dashboard 冒烟参考 | `tests/e2e/test_dashboard_smoke.py` |
| MCP 冒烟参考 | `tests/e2e/test_mcp_client.py` |

## 核心验收

| 验收项 | 参考命令 | 通过标准 |
|---|---|---|
| 成功摄取 1 个 PDF | `python scripts/ingest.py --path tests/fixtures/sample_documents/simple.pdf --collection demo --force` | 输出出现 `Pipeline completed successfully` 或汇总中 `Successful: 1` |
| 成功查询 1 个已摄取文档 | `python scripts/query.py --query "这个文档讲了什么？" --collection demo --verbose` | 最终 `RESULTS` 中至少返回 1 条结果 |
| 查询输出包含关键字段 | 同上 | 单条结果里包含 `score`、`source_path`、`chunk_index` |
| `tools/list` 返回 3 个工具 | `pytest tests/e2e/test_mcp_client.py -k initialize_and_tools_list -v` | 返回 `query_knowledge_hub`、`list_collections`、`get_document_summary` |
| `query_knowledge_hub` 返回可用结果 | `pytest tests/e2e/test_mcp_client.py -k query_knowledge_hub -v` | `tools/call` 返回合法 MCP `content` 块，不出现协议级错误 |
| Dashboard 有 6 页 | `pytest tests/e2e/test_dashboard_smoke.py -v` | 六个页面的 smoke test 全部通过 |
| 摄取 Trace 被持久化 | 运行 ingest 后查看 `logs/traces.jsonl` | 至少有一条 `trace_type=ingestion` 记录 |
| 查询 Trace 被持久化 | 运行 query 后查看 `logs/traces.jsonl` | 至少有一条 `trace_type=query` 记录 |
| `evaluate.py` 能输出报告 | `python scripts/evaluate.py --test-set tests/fixtures/golden_test_set.json --collection demo` | 输出 `EVALUATION REPORT` 或 JSON 报告 |

## 回归验收

| 回归项 | 参考验证方式 | 通过标准 |
|---|---|---|
| 重复摄取具备幂等性 | 同一 PDF 连续 ingest 两次，第二次不加 `--force` | 第二次显示 `skip` 或等价跳过行为 |
| Dense 单路回退可用 | 人为让 Sparse 失败，保留 Dense | 查询仍返回结果或稳定空结果，不崩溃 |
| Sparse 单路回退可用 | 人为让 Dense 失败，保留 Sparse | 查询仍返回结果或稳定空结果，不崩溃 |
| Rerank 失败能平稳回退 | 开启 rerank 后模拟 backend 异常 | 系统回退到 fusion 顺序，不整体报错 |
| 无 Vision 配置不影响摄取主链路 | `vision_llm.enabled=false` 后 ingest 含图 PDF | 文档仍可摄取，图片 caption 可为空 |
| 无 Ragas 配置不影响主系统运行 | 不装或不启用 Ragas | ingest/query/dashboard 仍可正常使用 |

## 复现完成判定

当复现版满足下面三层标准，就算完成 Phase 0 约定的验收目标：

1. 最小闭环：`ingest -> query`
2. 对外接口：MCP 3 工具 + Dashboard 6 页
3. 可观测性：trace + evaluation 至少有最简闭环
